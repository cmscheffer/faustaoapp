<div class="container">
  <div class="d-flex justify-content-between align-items-center">
    <h1>ORGANS ROBOTICOS</h1>
    <%= link_to "New Offer", new_organ_path, class: "btn btn-primary" %>
  </div>

  <div class="row justify-content-center">
    <div class="col-sm-8 my-3">
      <%= form_with url: organs_path, method: :get, class: "d-flex" do %>
        <%= text_field_tag :query,
              params[:query],
              class: "form-control",
              placeholder: "Type a keyword"
          %>
        <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
      <% end %>
  </div>
</div>

  <div class="cards row justify-content-center gap-5">
    <% @organs.each do |organ| %>
          <%= render 'partials/card', organ: organ %>
    <% end %>
  </div>
</div>



<%# if action_name == "index" && controller_name == "organs" %>
